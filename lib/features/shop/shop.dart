import 'package:flutter/material.dart';

class Shopp extends StatelessWidget {
  const Shopp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bienvenue"),
      ),
    );
  }
}